spring:
  web:
    resources:
      static-locations: classpath:/static/
  thymeleaf:
    cache: false
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/gestao_pedidos_db?useTimezone=true&serverTimezone=UTC
    username: root
    password: adminadmin123
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    properties:
      show_sql: false
